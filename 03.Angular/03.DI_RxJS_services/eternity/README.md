# Eternity

## Development server

Run `ng serve` for a dev server. Navigate to `http://localhost:4200/`. The app will automatically reload if you change any of the source files.

## Eternity customization

### Bootstrap is installed and registered in the angular.json file.

### The default 'app' prefix  was replaced with 'ett' in the tslint.json and angular.json files
