// from the package.json file run my-command and you will see all 3 commands executed
// npm is automatically executing same command names starting with pre/post respectively before and after the main command

console.log(process.env.MY_VAR);
